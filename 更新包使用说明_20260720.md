# 物理练习网站更新包使用说明（2026-07-20）

本次更新增加教师端“题库管理”：教师可上传标准 ZIP 练习包、暂时下架、重新发布或永久删除练习。
永久删除会同时删除该练习的题目、学生作答、得分、评语和统计，但不会删除学生账号。
学生端和教师端均可搜索练习；服务器管理员也可以通过交互式命令创建新的教师账号。

更新脚本会在覆盖程序前备份数据库、教师上传内容和旧版程序。现有账号、答案、分数和评语会保留。

## 一、从 Windows 上传

在 PowerShell 中运行：

```powershell
scp -P 12345 -i $env:USERPROFILE\.ssh\physics_server_ed25519 `
  "C:\aiCode\hm3\PAGE\release\physics-practice-update-20260720.tar.gz" `
  nyx@47.99.83.207:/tmp/
```

如果已经在 SSH 配置中指定了密钥，可以省略 `-i ...`。

## 二、在服务器更新

通过 SSH 登录：

```powershell
ssh -p 12345 -i $env:USERPROFILE\.ssh\physics_server_ed25519 `
  nyx@47.99.83.207
```

在 Ubuntu 服务器中运行：

```bash
rm -rf /tmp/physics-practice-update
mkdir -p /tmp/physics-practice-update
tar -xzf /tmp/physics-practice-update-20260720.tar.gz \
  -C /tmp/physics-practice-update
sudo bash /tmp/physics-practice-update/deploy/update-ubuntu-24.sh
```

## 三、验证

```bash
sudo systemctl is-active physics-practice
curl http://127.0.0.1:10123/api/health
sudo nginx -t
```

预期结果：

- 服务状态为 `active`；
- 健康检查返回包含 `"ok":true` 的 JSON；
- Nginx 配置检查显示成功。

然后访问 `http://47.99.83.207`，教师登录后打开“题库管理”。

如需新增教师账号：

```bash
sudo bash /opt/physics-practice/deploy/create-teacher.sh
```

按提示输入新用户名和两次密码。密码不会显示，无需重启网站。

## 四、新增练习

先下载教师页面提供的模板，或查看：

- `新增练习包格式.md`
- `examples/exercise-package/`

练习包必须是 ZIP，根目录包含：

```text
exercise.json
questions.md
answers.md
assets/            # 可选
```

首次上传建议保持草稿状态。确认题目、答案、单位、小数位数和分值无误后再发布。

## 五、出现问题时

查看最近日志：

```bash
sudo journalctl -u physics-practice -n 100 --no-pager
sudo journalctl -u nginx -n 50 --no-pager
```

更新脚本若在健康检查前失败，会自动恢复上一版程序；数据库不会被新程序包覆盖。
