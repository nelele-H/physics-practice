# 新增练习 ZIP 格式

教师端“题库管理”上传一个 ZIP 文件。文件结构：

```text
exercise-package.zip
├─ exercise.json
├─ questions.md
├─ answers.md
└─ assets/              # 可选
   ├─ graph-01.png
   └─ diagram-02.jpg
```

ZIP 最大 10 MB，解压后最大 25 MB。素材只接受 PNG、JPG、JPEG、GIF 和 WebP，不接受
SVG、HTML 或脚本文件。

## exercise.json

```json
{
  "formatVersion": 1,
  "slug": "topic2a-waves",
  "code": "2A",
  "title": "Topic 2A · Waves 波",
  "subtitle": "波速、频率、波长与波的性质",
  "expectedPoints": 60,
  "timeMinutes": 75,
  "publishImmediately": false
}
```

- `slug`：全站唯一，只用小写字母、数字和连字符；永久删除时需要输入它确认。
- `code`：学生和教师看到的短代码。
- `expectedPoints`：预期总分；系统会和所有可作答小题求和比较。
- `publishImmediately`：建议保持 `false`，导入后先预览，再在教师端发布。

## questions.md

整题格式：

```markdown
### Question 1 [1] {id=q1 type=mcq}
```

带小问格式：

```markdown
### Question 2 [5]

共同题干。

#### (a) [2] {id=q2a type=numeric unit="m s⁻¹" decimals=1}

小问题干。

#### (b) [3] {id=q2b type=manual}

小问题干。
```

规则：

- `id` 在一套练习中必须唯一且长期稳定；
- `type=mcq` 必须恰好提供 A、B、C、D 四个选项；
- `type=numeric` 必须提供 `unit` 和 `decimals`；
- `type=text` 用于可自动判分的文本填空；题干中的每个 `___` 会变成一个单行输入框；
- 文本填空默认区分大小写；需要忽略大小写时写 `case-sensitive=false`；
- `type=manual` 用于问答、解释、实验、评价和需要教师给分的题；
- 父题分值必须等于各小问分值之和；
- 题目中直接显示图片写作 `![说明](assets/graph-01.png)`；图片会自动适应电脑和手机宽度，点击可查看大图；
- 需要同时提供原图入口时，在下一行写 `[点击查看原图](assets/graph-01.png)`；点击后同样在当前页面查看大图；
- 建议把图片放入 ZIP 的 `assets/`，不要依赖可能失效或限制访问的外部图片网址；
- 简短行内公式建议写在反引号中，例如 `` `v = s/t` ``；
- 需要正式排版的公式使用独立的 `$$...$$`。

## answers.md

每个答案标题用相同 `id` 对应题目。

选择题：

```markdown
### Question 1 {id=q1}

Correct: B

答案解释。
```

数值题：

```markdown
#### (a) {id=q2a}

Correct: 12.0
Tolerance: 0.05

完整计算和解析。
```

`Correct` 只写数值，不写单位；小数位数必须与题目中的 `decimals` 一致。科学计数法使用
`1.1*10^2`。`Tolerance` 可省略，省略时系统按最后一位的半个单位自动生成容差。

文本填空题：

```markdown
### Question 3 [2] {id=q3 type=text case-sensitive=false}

波速等于频率乘以 ___；符号公式为 `v = ___ λ`。
```

对应答案使用单行 JSON 数组，数组项顺序与题干中的空格一致。一个空允许多个答案时使用嵌套数组：

```markdown
### Question 3 {id=q3}

Correct-Text: [["波长", "wavelength"], ["f"]]

答案解析。
```

学生只在每个空格的输入框中填写缺少的词或词组。系统会去除首尾空格并合并连续空格，再逐空
比较；不会自动忽略标点。每个空都正确时获得该题满分，否则为 0 分。

主观题：

```markdown
#### (b) {id=q2b}

Scoring:

- 1 分：得分点一；
- 1 分：得分点二；
- 1 分：得分点三。
```

## 导入与删除

导入时系统先完成全部检查，任何一项不通过都不会写入数据库。导入成功后可以保存为草稿或直接
发布。

永久删除前系统会创建 SQLite 备份，然后在同一个数据库事务中删除练习、题目、学生作答、
评分、评语和统计。学生账号不会删除。删除记录会阻止内置练习在服务重启后重新出现。
