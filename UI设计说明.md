# 小小物理站 UI 设计说明

## 1. 设计目标

第一版界面面向初中生和教师，优先满足：

- 一眼看懂当前页面能做什么；
- 学生答题时减少干扰；
- 教师在账号、批改和统计之间快速切换；
- 手机和电脑均可使用；
- 不依赖大型前端框架、图片素材或复杂动画。

视觉关键词：

> 清楚、温和、可靠、稍微可爱、不过度幼稚。

## 2. 色彩方向

采用“莫兰迪紫色到白色”为主色阶，降低饱和度，避免长时间阅读疲劳。

| 用途 | 色值 | 说明 |
|---|---|---|
| 深紫 | `#493F50` | 大标题、深色背景 |
| 主题紫 | `#65596D` | 主按钮、品牌标记 |
| 中紫 | `#796B81` | 次级强调 |
| 灰紫 | `#9B8DA2` | 输入框焦点、辅助信息 |
| 浅紫 | `#D9D0DB` | 边框 |
| 极浅紫 | `#F6F2F6` | 页面背景 |
| 白色 | `#FFFFFF` | 卡片和主要阅读区域 |
| 鼠尾草绿 | `#9EAA99` | 正确、成功 |
| 柔和陶粉 | `#C99F96` | 错误、删除、未得满分 |
| 柔和琥珀 | `#B18C57` | 批改中、提醒 |

颜色不是唯一的信息来源。正确、错误和批改状态同时使用文字及符号表示。

## 3. 字体与字号

- 中文：`Microsoft YaHei`、`PingFang SC`、`Noto Sans CJK SC`；
- 正文默认使用系统无衬线字体，减少额外下载；
- 页面主标题：30～46 px；
- 卡片标题：20～24 px；
- 正文：默认 16 px；
- 辅助信息：12～14 px；
- 公式：使用 KaTeX 服务端生成，保持数学符号和上下标清晰。

## 4. 形状与可爱感

- 卡片圆角 18 px，主要容器圆角 24～32 px；
- 按钮圆角 12 px；
- 品牌标记使用纯色圆角方块和 `φ`；
- 登录页使用纯色渐变、圆形和不规则 CSS 色块；
- 不使用复杂插画、动态背景或卡通人物；
- 动画仅限按钮轻微上移和消息出现，并尊重系统“减少动态效果”设置。

## 5. 学生端

### 登录与激活

- 登录页只有用户名、密码和登录按钮；
- 学生激活页分开，避免普通登录页面过于复杂；
- 激活码、用户名和密码按自然顺序排列；
- 错误信息在表单内明确显示，不只依赖弹窗。

### 练习列表

- 每套练习是一张独立卡片；
- 显示练习状态、题量、已写题数和已发布成绩；
- 使用“未开始、可继续作答、批改中、已发布成绩”等完整中文。

### 答题页

- 左侧或顶部提供“全部、未作答、已作答、错题”筛选；
- 每个可评分小题是一张独立卡片；
- 选择题使用大面积可点击选项；
- 直接计算题使用单行数值输入框，并在输入框旁显示指定单位；
- 数值题上方用标签明确显示“单位”和“保留几位小数”，下方提示科学计数法写作 `1.1*10^2`；
- 学生只填写数值，不在答案中重复填写单位；
- 主观题使用较大的多行输入框；
- “删除答案”和“保存答案”明确分开；
- 教师锁定后，输入框禁用并显示锁定说明；
- 发布成绩后，满分题不显示标准答案；
- 未得满分题显示得分、教师评语、正确答案和解析。

## 6. 教师端

教师端使用四个一级标签：

1. 学生账号；
2. 作答与批改；
3. 逐题统计；
4. 题库管理。

### 学生账号

- 激活码创建和直接创建账号并列；
- 学生表格提供改用户名、重置密码、强制退出、停用/启用；
- 教师不能查看学生原密码，只能重置。

### 作答与批改

- 先选择练习，再查看学生作答；
- 可勾选多个学生批量锁定；
- 每个学生也有“锁定并批改”入口；
- 批改卡片同时显示题目、学生答案、参考答案和评分控件；
- 选择题和数值填空题由系统按“全对得满分、错误得 0 分”自动判分；
- 主观题得分必须由教师输入；
- 每题有评语框，页面底部有总体评语；
- “保存批改”和“保存并发布成绩”分开。

### 逐题统计

- 表格显示题号、满分、作答人数、已评分人数、完全正确人数、正确率和平均得分率；
- 只有百分比没有意义，因此人数与百分比同时显示；
- 空数据使用横线，不显示误导性的 `0%`。

### 题库管理

- 上传区只接受标准 ZIP 练习包，并提供可下载模板；
- 导入前自动检查题型、答案、总分、单位、小数位数、公式和素材类型；
- 题库表显示来源、发布状态、题量、总分、作答人数、答案数和评分数；
- “下架”保留全部历史数据，“永久删除”使用危险色；
- 永久删除前先显示影响数量，再要求输入完整 slug 二次确认；
- 系统在真正删除前自动生成数据库备份。

### 练习搜索

- 学生端在练习列表上方提供搜索，匹配练习编号、标题、副标题和 slug；
- 教师端搜索常驻工作台，搜索范围包含已发布、草稿和已下架练习；
- 教师可从搜索结果直接进入学生作答、逐题统计或题库管理；
- 没有结果时显示明确提示，不改变或删除原有练习数据。

## 7. 响应式设计

- 880 px 以下：答题侧栏移动到题目上方，批改双栏变单栏；
- 680 px 以下：练习卡片、统计卡片和表单全部单列；
- 按钮高度不小于 34 px，主要按钮不小于 42 px；
- 表格允许横向滚动，不压缩到无法阅读；
- 所有输入框均带文字标签。

## 8. 第一版不做的 UI

- 不做排行榜、勋章和积分动画；
- 不做复杂图表；
- 不做拖拽题和手写画板；
- 不做可更换主题；
- 不做大型侧边栏后台；
- 不做 AI 聊天入口；
- 不为后期计时考试提前显示倒计时控件。

这些限制用于保持页面清楚、运行轻量，并把开发精力集中在账号、答题、批改和成绩闭环。
